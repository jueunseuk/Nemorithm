package returns.nemorithm.solver;

import returns.nemorithm.domain.nonogram.Board;
import returns.nemorithm.domain.nonogram.LineSegment;
import returns.nemorithm.domain.nonogram.Position;

import java.util.List;

public class CompleteLine {
    public static List<Position> complete(Board board, LineSegment lineSegment) {
        return null;
    }

    public static boolean isComplete(Board board, LineSegment lineSegment) {
        return false;
    }
}
